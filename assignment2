arr=[1,4,20,3,10,5]
sum=33
d={}
n=len(arr)
curr_sum=0
for i in range(n):
    curr_sum+=arr[i]
    if curr_sum==sum:
        print(0,i)
    if curr_sum-sum in d:
        print(d[curr_sum-sum],i)
    if curr_sum in d:
        d[curr_sum]+=1
    else:
        d[curr_sum]=1
        
        
       
